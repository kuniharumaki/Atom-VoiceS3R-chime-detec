# 🤖 AGENTS.md

このファイルは、本プロジェクトを操作・改修する AI エージェントのためのコンテキストおよび制約事項をまとめたものです。

## 概要 (Project Context)
本プロジェクト `Atom-VoiceS3R-chime-detect` は、ESP32-S3 (Atom VoiceS3R) を用いてインターホンのチャイム音を検知するためのファームウェアです。
内蔵の I2S マイクから音声データを取得し、FFT を用いて特定の周波数パターン（Ding: 652Hz -> Dong: 513Hz）を検知します。
また、検知結果は OLED ディスプレイ（SH1107）に表示され、MQTT 経由でネットワーク上のブローカーに JSON フォーマットで通知されます。

## ハードウェアの制約事項 (Hardware Constraints)
コードを修正する際は、以下のハードウェアに関する制約を必ず守ってください。

1. **I2C バスの競合回避 (最重要)**:
   - `M5Atomic-EchoBase` のコーデック (ES8311) は `Wire` (SDA: 45, SCL: 0) を使用して初期化されます。
   - 外部接続の OLED モジュール (Grove 端子) は、**必ず `Wire1` (SDA: 2, SCL: 1)** を使用する必要があります。
   - OLED 初期化時に `Wire` を使用すると、コーデックと競合してマイク入力が機能しなくなります。

2. **初期化の順序**:
   - `Wire` および `Wire1` の初期化タイミングに注意してください。
   - `M5EchoBase` の `init()` を呼び出す前に、OLED (M5GFX) の初期化を完了させる必要があります（依存ライブラリ内部での I2C 初期化競合を避けるため）。

3. **OLED の仕様 (SH1107)**:
   - この OLED モジュールは白黒（モノクロ）仕様です。
   - M5GFX 側で `canvas.setColorDepth(1);` を設定しており、描画色は必ず `TFT_WHITE` または `TFT_BLACK` のみを使用してください。他の色を指定すると表示されません。
   - OLED は常時接続されるとは限りません（起動時のアドレス `0x3C` 応答確認によるオートディテクト機能を実装済み）。OLED が無い前提でもハングアップせずに動作する設計を維持してください。

## 開発環境・プロジェクト構成
- **PlatformIO**: 本プロジェクトは PlatformIO 環境で管理されています (`platformio.ini`)。
- **マルチタスク設計**: 
  - `i2sRecordTask` (Core 1, 優先度3): I2S バッファの読み取り
  - `monitorTask` (Core 0, 優先度2): FFT 解析とステートマシン、MQTT 送信フラグの設定
  - `oledDisplayTask` (Core 0, 優先度1): 画面描画
  - `loop` (Core 1): MQTT のキープアライブとパブリッシュ処理

## 機密情報の管理 (Secrets Management)
- `src/wifi_config.h` は Wi-Fi の SSID/パスワード および MQTT ブローカーの設定情報を保持するため、`.gitignore` でトラッキングから除外されています。
- エージェントは、コミットやログ出力においてこれらの情報を意図せず露出させないように注意してください。
